"""PlantStar TTS - offline text-to-speech service for the PlantStar APU server.

Wraps the Piper neural TTS engine (CPU-only, fully offline) and exposes it
through both an HTTP API and a command-line interface, with multiple
selectable voice profiles across several languages.
"""

__version__ = "1.0.0"
